package com.nn.mindweb.server

object TypeDefs {
  //вообще следующей строчки быть не должно но скалакомпилятор сошел с ума (она не имеет значения вообще)
  //похоже пустой объект (без этой строчки) игнорировался компилятором
	type R = Request
	
	val tiny = false
}